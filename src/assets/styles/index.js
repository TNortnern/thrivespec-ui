// just some global styles to keep things easier and consistent
import colorStyles from "./colorStyles"
import layoutStyles from "./layoutStyles";
import utilityStyles from "./utilityStyles";
import textStyles from "./textStyles";
import inputStyles from "./inputStyles";

export const colors = { ...colorStyles };
export const layout = { ...layoutStyles };
export const utility = { ...utilityStyles };
export const text = { ...textStyles };
export const input = { ...inputStyles };
